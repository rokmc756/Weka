- hosts: all
  become: yes
  vars:
    print_debug: true
    unconfig_client: false
    delete_wekafs: false
    destroy_weka: false
    uninstall_binary: false
    disable_obs: false
    disable_s3: true
  roles:
    - { role: weka }
    # - { role: firewalld }
    #    - { role: init-hosts }
