#!/usr/bin/expect -f
#
set username [lindex $argv 0]
set password [lindex $argv 1]

set timeout 5

        spawn /usr/bin/weka user delete $username

        expect  "^Password for*"
        send    "$password\r"

        interact

exit
